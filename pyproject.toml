[tool.poetry]
name = "sse-test"
version = "0.1.0"
description = ""
authors = ["Grant Griffiths <grant@neubird.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
